services:
  consensus-engine:
    build:
      context: .
    ports:
      - "127.0.0.1:${PORT:-8080}:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
